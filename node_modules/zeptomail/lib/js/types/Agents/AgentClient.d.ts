import { BaseModel } from "../BaseModel/BaseModel";
import type { AddAgentDataType, UpdateAgentDataType, AgentKeyParam, RevokeAPIKeyParam, DeleteSMTPPasswordParam, GetAllAgentsResponse, AgentDetailResponse, GetAPIKeysResponse, GenerateAPIKeyResponse, GetSMTPListResponse, GenerateSMTPPasswordResponse } from "./types";
declare class AgentClient extends BaseModel {
    basePath: string;
    /** Returns all mail agents associated with the account. */
    getAllAgents(): Promise<GetAllAgentsResponse>;
    /**
     * Creates a new mail agent.
     *
     * @param agentName - Display name for the agent.
     * @param description - Brief description of the agent's purpose.
     */
    addAgent(agentData: AddAgentDataType): Promise<AgentDetailResponse>;
    /**
     * Updates an existing mail agent's name and description.
     *
     * @param agentKey - Unique key of the agent to update.
     * @param agentName - New display name.
     * @param description - New description.
     */
    updateAgent(agentData: UpdateAgentDataType): Promise<AgentDetailResponse>;
    /**
     * Permanently deletes a mail agent.
     *
     * @param agentKey - Unique key of the agent to delete.
     *
     * NOTE:
     * Only the shutdowned agents can be deleted.
     */
    deleteAgent({ agentKey }: AgentKeyParam): Promise<void>;
    /**
     * Lists all API keys for the specified agent.
     *
     * @param agentKey - Unique key of the agent.
     */
    getAPIKeys({ agentKey }: AgentKeyParam): Promise<GetAPIKeysResponse>;
    /**
     * Generates a new API key for the specified agent.
     *
     * @param agentKey - Unique key of the agent.
     */
    generateAPIKey({ agentKey }: AgentKeyParam): Promise<GenerateAPIKeyResponse>;
    /**
     * Revokes an existing API key for an agent.
     *
     * @param agentKey - Unique key of the agent.
     * @param id - The ID of the API key to revoke.
     */
    revokeAPIKey({ agentKey, id }: RevokeAPIKeyParam): Promise<void>;
    /**
     * Lists all SMTP passwords for the specified agent.
     *
     * @param agentKey - Unique key of the agent.
     */
    getSMTPList({ agentKey }: AgentKeyParam): Promise<GetSMTPListResponse>;
    /**
     * Generates a new SMTP password for the specified agent.
     *
     * @param agentKey - Unique key of the agent.
     */
    generateSMTPPassword({ agentKey }: AgentKeyParam): Promise<GenerateSMTPPasswordResponse>;
    /**
     * Deletes an SMTP password for the specified agent.
     *
     * @param agentKey - Unique key of the agent.
     * @param passwordId - ID of the SMTP password to delete.
     */
    deleteSMTPPassword({ agentKey, passwordId }: DeleteSMTPPasswordParam): Promise<void>;
}
export { AgentClient };

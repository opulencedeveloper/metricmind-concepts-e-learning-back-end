type WithAuthClient = {
    authenticationClient: object | null;
    constructor: {
        name: string;
    };
};
declare function requireAuthenticationClient(_target: object, _propertyKey: string, descriptor: PropertyDescriptor): PropertyDescriptor;
/**
 * Decorator factory that validates the named parameter in the first argument object is non-empty.
 * Rejects with a descriptive error before the method body runs.
 *
 * @example
 * \@requireParam("domainKey")
 * getDomainDetails({ domainKey }: { domainKey: string }) { ... }
 */
declare function requireParam(paramName: string): (_target: object, _propertyKey: string, descriptor: PropertyDescriptor) => PropertyDescriptor;
export { requireAuthenticationClient, requireParam, type WithAuthClient };

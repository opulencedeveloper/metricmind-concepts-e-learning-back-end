declare const DCs: readonly ["US", "IN", "EU", "AU", "JP", "CN"];
type DataCenter = typeof DCs[number];
type initialProps = {
    clientId: string;
    clientSecret: string;
    refreshToken: string;
    dataCenter?: DataCenter;
    timeout?: number;
    apiVersion?: string;
    bufferMs?: number;
};
type ClientUrls = {
    accountsUrl: string;
    apiBaseUrl: string;
};
type DataCentersList = {
    [key in DataCenter]: ClientUrls;
};
export type { initialProps, ClientUrls, DataCentersList, DataCenter };
